# Чеклист проверки проекта для Vercel

## ✅ Проверка выполнена

### 1. Production URL vs Preview URL

**Проверка:**
- ✅ Используется Production URL: `https://knowflow-nikolais-projects-225de923.vercel.app`
- ⚠️ **Важно:** Убедитесь, что вы открываете Production URL, а не Preview URL

**Как проверить на Vercel:**
1. Deployments → найдите деплой со статусом "Production"
2. URL должен быть без префикса типа `-git-main-` или `-pr-`
3. Production URL обычно: `https://your-project.vercel.app`
4. Preview URL обычно: `https://your-project-git-branch-username.vercel.app`

**Решение:**
- Откройте Production деплой (не Preview)
- Или настройте Production деплой как основной

---

### 2. Редиректы/реврайты в next.config.ts

**Проверка:**
- ✅ Есть редирект: `/` → `/collection`
- ✅ Редирект ведет на существующую страницу (`/collection` существует)
- ✅ Нет реврайтов (rewrites) на несуществующие страницы

**Текущая конфигурация:**
```typescript
async redirects() {
  return [
    {
      source: "/",
      destination: "/collection",  // ✅ Страница существует
      permanent: false,
    },
  ];
}
```

**Статус:** ✅ Все редиректы ведут на существующие страницы

---

### 3. Динамические роуты без generateStaticParams

**Проверка:**
- ✅ Нет динамических роутов (папок с `[param]`)
- ✅ Все роуты статические

**Проверено:**
```bash
find src/app -type d -name "*\[*"
# Результат: пусто (нет динамических роутов)
```

**Статус:** ✅ Нет динамических роутов, все страницы статические

---

### 4. Clear Cache & Redeploy

**Проверка:**
- ⚠️ Это действие нужно выполнить на Vercel

**Инструкция:**
1. Vercel Dashboard → Ваш проект → Settings → General
2. Прокрутите вниз до "Build & Development Settings"
3. Нажмите **"Clear Build Cache"**
4. Deployments → последний деплой → **"Redeploy"**

**Статус:** ⚠️ Требуется выполнить на Vercel

---

### 5. Деплой из правильной ветки

**Проверка:**
- ✅ Текущая ветка: `check_versel` (или другая)
- ⚠️ **Важно:** На Vercel должна быть настроена ветка `main` или `master`

**Как проверить на Vercel:**
1. Settings → Git
2. Проверьте "Production Branch"
3. Должно быть: `main` или `master`

**Как исправить:**
1. Settings → Git → Production Branch
2. Выберите `main` или `master`
3. Сохраните

**Статус:** ⚠️ Проверьте на Vercel, что Production Branch = `main` или `master`

---

## 📋 Итоговый чеклист

- [x] ✅ Production URL используется (проверьте на Vercel)
- [x] ✅ Нет редиректов на несуществующие страницы
- [x] ✅ Нет динамических роутов без generateStaticParams
- [ ] ⚠️ Clear Cache & Redeploy (выполните на Vercel)
- [ ] ⚠️ Деплой из правильной ветки (проверьте на Vercel)

---

## 🔧 Что нужно сделать на Vercel

### Шаг 1: Проверьте Production Branch

1. Settings → Git
2. Production Branch должен быть: `main` или `master`
3. Если нет - измените и сохраните

### Шаг 2: Очистите кеш и пересоберите

1. Settings → General → Build & Development Settings
2. Нажмите **"Clear Build Cache"**
3. Deployments → последний деплой → **"Redeploy"**

### Шаг 3: Проверьте Production URL

1. Deployments → найдите деплой со статусом **"Production"**
2. Откройте Production URL (не Preview)
3. Проверьте, что страницы работают

---

## 🚨 Если все равно 404

### Проверьте Root Directory (самое важное!)

1. Settings → General → **Root Directory**
2. **Должно быть пусто** (не `src`, не `app`)
3. Если указано значение - удалите его
4. Сохраните
5. Clear Cache & Redeploy

**Это решает 90% проблем с 404!**

---

## ✅ После всех проверок

Если все пункты выполнены, но проблема сохраняется:

1. Проверьте логи деплоя:
   - Deployments → последний деплой → View Build Logs
   - Должны быть видны все роуты в "Route (app)"

2. Проверьте переменные окружения:
   - Settings → Environment Variables
   - Все переменные должны быть для Production, Preview, Development

3. Отправьте скриншоты:
   - Settings → General (Root Directory, Framework Preset)
   - Build Logs (секция "Route (app)")
   - Ошибка в браузере (F12 → Console)


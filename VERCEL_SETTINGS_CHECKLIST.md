# Чеклист настроек Vercel для исправления 404

## ✅ Критически важные настройки

### 1. Root Directory
**Где:** Settings → General → Root Directory

**Должно быть:**
- Пусто (`.`)
- ИЛИ не указано вообще

**НЕ должно быть:**
- `src`
- `app`
- Любая другая подпапка

---

### 2. Framework Preset
**Где:** Settings → General → Framework Preset

**Должно быть:**
- `Next.js`

**Если не Next.js:**
- Выберите "Other" → "Next.js"
- Или удалите проект и создайте заново с правильным пресетом

---

### 3. Build Command
**Где:** Settings → General → Build & Development Settings → Build Command

**Должно быть:**
- `npm run build`
- ИЛИ пусто (Vercel определит автоматически)

---

### 4. Output Directory
**Где:** Settings → General → Build & Development Settings → Output Directory

**Должно быть:**
- `.next`
- ИЛИ пусто (Vercel определит автоматически)

**НЕ должно быть:**
- `dist`
- `build`
- `out`
- Любое другое значение

---

### 5. Install Command
**Где:** Settings → General → Build & Development Settings → Install Command

**Должно быть:**
- `npm install`
- ИЛИ пусто (Vercel определит автоматически)

---

## 🔍 Проверка структуры проекта

Убедитесь, что структура правильная:

```
knowflow/
├── src/
│   └── app/
│       ├── layout.tsx          ✅ Обязательно
│       ├── page.tsx            ✅ Обязательно (главная страница)
│       ├── login/
│       │   └── page.tsx        ✅
│       ├── collection/
│       │   └── page.tsx        ✅
│       └── ...
├── next.config.ts              ✅
├── package.json                ✅
└── vercel.json                 ✅ (опционально)
```

---

## 🚨 Частые проблемы

### Проблема 1: Root Directory указан неправильно

**Симптомы:**
- Все страницы возвращают 404
- Локально всё работает

**Решение:**
1. Settings → General → Root Directory
2. Оставьте пустым или установите `.`
3. Сохраните
4. Сделайте новый деплой

---

### Проблема 2: Framework Preset не Next.js

**Симптомы:**
- Vercel не распознает проект как Next.js
- Сборка проходит, но роуты не работают

**Решение:**
1. Settings → General → Framework Preset
2. Выберите "Next.js"
3. Сохраните
4. Сделайте новый деплой

---

### Проблема 3: Output Directory указан неправильно

**Симптомы:**
- Сборка проходит, но файлы не находятся

**Решение:**
1. Settings → General → Output Directory
2. Оставьте пустым или установите `.next`
3. Сохраните
4. Сделайте новый деплой

---

## 📋 Пошаговая проверка

### Шаг 1: Откройте настройки проекта
1. Vercel Dashboard → Ваш проект `knowflow`
2. Settings → General

### Шаг 2: Проверьте Root Directory
- [ ] Root Directory: пусто или `.`
- [ ] НЕ указана подпапка `src` или `app`

### Шаг 3: Проверьте Framework
- [ ] Framework Preset: `Next.js`

### Шаг 4: Проверьте Build Settings
- [ ] Build Command: `npm run build` или пусто
- [ ] Output Directory: `.next` или пусто
- [ ] Install Command: `npm install` или пусто

### Шаг 5: Проверьте переменные окружения
- [ ] `NEXT_PUBLIC_SUPABASE_URL` добавлена
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` добавлена
- [ ] `TELEGRAM_BOT_TOKEN` добавлена
- [ ] `TELEGRAM_CRON_SECRET` добавлена

### Шаг 6: Очистите кеш
1. Settings → General
2. Прокрутите вниз до "Build & Development Settings"
3. Нажмите "Clear Build Cache"
4. Сохраните

### Шаг 7: Сделайте новый деплой
1. Deployments → "Redeploy"
2. ИЛИ сделайте commit и push:
   ```bash
   git commit --allow-empty -m "Trigger Vercel redeploy"
   git push origin main
   ```

---

## 🔧 Альтернативное решение

Если ничего не помогает:

### Вариант 1: Пересоздать проект
1. Удалите проект из Vercel
2. Создайте новый проект
3. Импортируйте репозиторий
4. **Важно:** При создании выберите "Next.js" как Framework
5. Добавьте переменные окружения
6. Задеплойте

### Вариант 2: Использовать CLI
```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Задеплойте
vercel --prod
```

---

## ✅ Проверка после исправления

После изменения настроек:

1. **Проверьте логи деплоя:**
   - Deployments → последний деплой → View Build Logs
   - Должны быть видны все роуты в секции "Route (app)"

2. **Проверьте URL:**
   - Откройте `https://your-app.vercel.app/login`
   - Должна открыться страница логина (не 404)

3. **Проверьте консоль браузера:**
   - F12 → Console
   - Не должно быть ошибок 404

---

## 📞 Если проблема сохраняется

1. **Проверьте логи деплоя:**
   - Скопируйте логи из Vercel
   - Проверьте, есть ли ошибки

2. **Проверьте локальную сборку:**
   ```bash
   npm run build
   npm run start
   ```
   - Откройте `http://localhost:3000/login`
   - Должно работать

3. **Сравните настройки:**
   - Локально: работает ✅
   - Vercel: не работает ❌
   - Значит проблема в настройках Vercel

---

## 🎯 Самый частый случай

**90% проблем решается так:**

1. Settings → General → Root Directory → **Оставить пустым**
2. Settings → General → Framework Preset → **Next.js**
3. Clear Build Cache
4. Redeploy

Попробуйте это в первую очередь!


# Настройка Telegram бота для флеш-карточек

## Обзор

Telegram бот позволяет получать вопросы из флеш-карточек прямо в Telegram и изучать их с помощью адаптивных интервалов повторения (алгоритм SM-2).

## Функции

- 📚 Автоматическая отправка карточек по расписанию
- ⏰ Настраиваемое время уведомлений
- 📊 Статистика изучения
- 🎯 Адаптивные интервалы повторения (SM-2)
- ⚙️ Настройки лимита карточек в день

## Установка

### 1. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен бота

### 2. Настройка переменных окружения

Добавьте в `.env.local`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CRON_SECRET=your_secret_key_for_cron_jobs
```

### 3. Создание таблицы в Supabase

Выполните SQL миграцию:

```sql
-- Файл: supabase/migrations/create_telegram_users_table.sql
```

Или выполните SQL в Supabase Dashboard → SQL Editor.

### 4. Установка webhook

После деплоя приложения установите webhook:

```bash
curl "https://your-domain.com/api/telegram/webhook?action=set-webhook&url=https://your-domain.com/api/telegram/webhook"
```

Или используйте Telegram Bot API напрямую:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-domain.com/api/telegram/webhook"}'
```

### 5. Настройка cron job для автоматической отправки

Настройте cron job для вызова endpoint отправки карточек:

```bash
# Каждый час проверяем и отправляем карточки
0 * * * * curl -X POST https://your-domain.com/api/telegram/send-cards \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

Или используйте сервисы типа:
- [Vercel Cron](https://vercel.com/docs/cron-jobs)
- [GitHub Actions](https://docs.github.com/en/actions)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)

## Использование

### Привязка аккаунта

1. Откройте приложение KnowFlow
2. Перейдите в Настройки → Telegram бот
3. Нажмите "Привязать Telegram"
4. Скопируйте код
5. Откройте бота в Telegram
6. Отправьте команду `/link <код>`

### Команды бота

- `/start` - Начать работу с ботом
- `/study` - Начать изучение карточек сейчас
- `/stats` - Показать статистику изучения
- `/settings` - Настройки бота
- `/help` - Справка по командам

### Изучение карточек

1. Бот автоматически отправляет карточки в установленное время
2. Или используйте команду `/study` для изучения сейчас
3. Нажмите "Показать ответ" для просмотра ответа
4. Оцените карточку:
   - ❌ Сложно (0) - карточка будет повторена через 1 день
   - 🤔 Нормально (3) - стандартный интервал
   - ✅ Легко (5) - интервал увеличивается

### Настройки

В `/settings` можно настроить:
- 🕐 Время уведомлений (формат HH:MM)
- 📊 Лимит карточек в день (1-50)
- ⏸/▶️ Включить/выключить уведомления

## API Endpoints

### POST `/api/telegram/webhook`
Webhook для получения обновлений от Telegram.

### POST `/api/telegram/send-cards`
Отправка карточек активным пользователям (вызывается по расписанию).

**Заголовки:**
```
Authorization: Bearer YOUR_CRON_SECRET
```

### POST `/api/telegram/generate-link`
Генерация кода привязки Telegram аккаунта.

**Тело запроса:**
```json
{
  "userId": "user-uuid"
}
```

**Ответ:**
```json
{
  "linked": false,
  "linkCode": "user-uuid",
  "instructions": "Отправьте боту команду: /link user-uuid"
}
```

## Структура базы данных

### Таблица `telegram_users`

| Поле | Тип | Описание |
|------|-----|----------|
| id | UUID | ID записи |
| user_id | UUID | ID пользователя из auth.users |
| telegram_user_id | BIGINT | ID пользователя в Telegram |
| telegram_username | TEXT | Username в Telegram |
| is_active | BOOLEAN | Активен ли бот для пользователя |
| notification_time | TIME | Время отправки уведомлений |
| timezone | TEXT | Часовой пояс |
| daily_limit | INTEGER | Лимит карточек в день |
| created_at | TIMESTAMPTZ | Дата создания |
| updated_at | TIMESTAMPTZ | Дата обновления |

## Алгоритм SM-2

Бот использует алгоритм SM-2 для адаптивных интервалов повторения:

- **Оценка 0 (Сложно)**: Интервал сбрасывается до 1 дня
- **Оценка 3 (Нормально)**: Стандартное увеличение интервала
- **Оценка 5 (Легко)**: Интервал увеличивается больше

Интервалы:
- 1-е повторение: 1 день
- 2-е повторение: 6 дней
- 3+ повторение: `интервал * easeFactor` дней

## Безопасность

1. **Webhook Secret**: Используйте секретный ключ для cron jobs
2. **RLS Policies**: Таблица `telegram_users` защищена RLS
3. **User Verification**: Проверка привязки через `user_id`

## Troubleshooting

### Бот не отвечает

1. Проверьте, что webhook установлен: `GET /api/telegram/webhook?action=set-webhook&url=...`
2. Проверьте логи в консоли
3. Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен

### Карточки не приходят автоматически

1. Проверьте, что cron job настроен
2. Проверьте `is_active` в таблице `telegram_users`
3. Проверьте время `notification_time`
4. Убедитесь, что есть карточки к повторению (`next_review <= NOW()`)

### Ошибка привязки аккаунта

1. Проверьте правильность кода
2. Убедитесь, что пользователь существует в `auth.users`
3. Проверьте логи в консоли

## Разработка

### Локальная разработка

Для локальной разработки используйте [ngrok](https://ngrok.com/) для туннелирования:

```bash
ngrok http 3000
```

Затем установите webhook на ngrok URL:

```bash
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-ngrok-url.ngrok.io/api/telegram/webhook"
```

### Тестирование

1. Создайте тестового бота через @BotFather
2. Используйте тестовую базу данных
3. Проверьте все команды бота
4. Протестируйте отправку карточек

## Дополнительные улучшения

- [ ] Поддержка нескольких языков
- [ ] Групповое изучение
- [ ] Статистика прогресса
- [ ] Экспорт/импорт карточек
- [ ] Категории карточек
- [ ] Напоминания о пропущенных карточках


# КРИТИЧЕСКАЯ ПРОБЛЕМА: Vercel использует Pages Router вместо App Router

## 🔍 Проблема

В логах Vercel видно:
```
Route (pages)
─ ○ /404
```

Вместо:
```
Route (app)
┌ ○ /
├ ○ /login
├ ○ /collection
└ ...
```

**Это значит, что Vercel использует Pages Router вместо App Router!**

---

## ✅ РЕШЕНИЕ: 3 критических шага

### Шаг 1: Root Directory (САМОЕ ВАЖНОЕ!)

**Это решает проблему в 90% случаев!**

1. **Vercel Dashboard** → Ваш проект → **Settings** → **General**
2. Найдите **"Root Directory"**
3. **КРИТИЧЕСКИ ВАЖНО:** Должно быть **ПУСТО** (не `src`, не `app`, просто пусто)
4. Если указано значение - **УДАЛИТЕ его** (оставьте полностью пустым)
5. Нажмите **"Save"**

**Почему это важно:**
- Если указано `src`, Vercel ищет `src/src/app/` вместо `src/app/`
- Next.js автоматически находит `src/app/` если Root Directory пустой
- Если Root Directory неправильный, Next.js не видит `src/app/` и использует Pages Router

---

### Шаг 2: Framework Preset

1. Settings → General → **"Framework Preset"**
2. Должно быть: **Next.js**
3. Если не Next.js → выберите **Next.js**
4. Сохраните

---

### Шаг 3: Очистите кеш и пересоберите

1. Settings → General → **"Build & Development Settings"**
2. Нажмите **"Clear Build Cache"**
3. Deployments → последний деплой → **"Redeploy"**

---

## 🔍 Проверка после исправления

После нового деплоя проверьте логи:

1. Deployments → последний деплой → **View Build Logs**
2. Прокрутите вниз до секции **"Route"**
3. **Должно быть:**
   ```
   Route (app)    ← ВОТ ЭТО!
   ┌ ○ /
   ├ ○ /_not-found
   ├ ƒ /api/generate-flashcards
   ├ ƒ /api/telegram/generate-link
   ├ ƒ /api/telegram/send-cards
   ├ ƒ /api/telegram/webhook
   ├ ○ /collection
   ├ ○ /graph
   ├ ○ /login
   ├ ○ /settings
   ├ ○ /trainer
   ├ ○ /trainer/cards
   ├ ○ /trainer/challenges
   ├ ○ /trainer/graph
   └ ○ /trainer/study
   ```

**НЕ должно быть:**
```
Route (pages)    ← ЭТО НЕПРАВИЛЬНО!
─ ○ /404
```

---

## 🚨 Почему это происходит

Next.js определяет, какой роутер использовать:

1. **Если есть папка `pages/`** → использует Pages Router
2. **Если есть папка `app/` или `src/app/`** → использует App Router
3. **Если Root Directory неправильный** → не видит `src/app/` → использует Pages Router → создает только `/404`

**В вашем случае:**
- Папки `pages/` нет ✅
- Папка `src/app/` есть ✅
- Но Root Directory, вероятно, указан как `src` ❌
- Поэтому Next.js не видит `src/app/` и использует Pages Router

---

## 📋 Чеклист

- [ ] Root Directory: **пусто** (не `src`, не `app`)
- [ ] Framework Preset: **Next.js**
- [ ] Build Command: `npm run build` или пусто
- [ ] Output Directory: `.next` или пусто
- [ ] Кеш очищен
- [ ] Новый деплой сделан
- [ ] В логах видно **"Route (app)"** (не "Route (pages)")

---

## 🎯 Самый частый случай (90%)

**Проблема:** Root Directory указан как `src`

**Решение:**
1. Settings → General → Root Directory
2. **Удалите значение** (оставьте пустым)
3. Сохраните
4. Clear Build Cache
5. Redeploy

**После этого в логах должно быть "Route (app)" вместо "Route (pages)"!**

---

## 📞 Если проблема сохраняется

Отправьте мне:
1. **Скриншот Settings → General** (Root Directory, Framework Preset)
2. **Полные логи сборки** (особенно секцию "Route")
3. **Структуру проекта** (`ls -la` в корне проекта)

С этой информацией я смогу точно определить проблему!


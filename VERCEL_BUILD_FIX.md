# Исправление: Vercel видит только роут 404

## 🔍 Проблема

В логах Vercel видно только установку зависимостей, но **НЕ видно секции "Route (app)"** с роутами. Это значит, что Next.js не видит структуру `src/app/`.

## ✅ РЕШЕНИЕ: Root Directory

**Это самая частая причина проблемы!**

### Шаг 1: Проверьте Root Directory на Vercel

1. **Vercel Dashboard** → Ваш проект → **Settings** → **General**
2. Найдите **"Root Directory"**
3. **КРИТИЧЕСКИ ВАЖНО:** Должно быть **ПУСТО** (не `src`, не `app`, просто пусто)
4. Если указано значение - **УДАЛИТЕ его** (оставьте полностью пустым)
5. Нажмите **"Save"**

**Почему это важно:**
- Если указано `src`, Vercel ищет `src/src/app/` вместо `src/app/`
- Next.js автоматически находит `src/app/` если Root Directory пустой

### Шаг 2: Framework Preset

1. Settings → General → **"Framework Preset"**
2. Должно быть: **Next.js**
3. Если не Next.js → выберите **Next.js**
4. Сохраните

### Шаг 3: Очистите кеш и пересоберите

1. Settings → General → **"Build & Development Settings"**
2. Нажмите **"Clear Build Cache"**
3. Deployments → последний деплой → **"Redeploy"**

---

## 🔍 Проверка после исправления

После нового деплоя проверьте логи:

1. Deployments → последний деплой → **View Build Logs**
2. Прокрутите вниз до секции **"Route (app)"**
3. **Должны быть видны все роуты:**
   ```
   Route (app)
   ┌ ○ /
   ├ ○ /_not-found
   ├ ƒ /api/generate-flashcards
   ├ ƒ /api/telegram/generate-link
   ├ ƒ /api/telegram/send-cards
   ├ ƒ /api/telegram/webhook
   ├ ○ /collection
   ├ ○ /graph
   ├ ○ /login
   ├ ○ /settings
   ├ ○ /trainer
   ├ ○ /trainer/cards
   ├ ○ /trainer/challenges
   ├ ○ /trainer/graph
   └ ○ /trainer/study
   ```

**Если роуты видны** → проблема решена! ✅
**Если роуты НЕ видны** → см. дополнительные шаги ниже

---

## 🚨 Дополнительные проверки

### Проверка 1: Структура проекта

Убедитесь, что структура правильная:
```
knowflow/
├── src/
│   └── app/
│       ├── layout.tsx          ✅
│       ├── page.tsx             ✅
│       ├── login/
│       │   └── page.tsx         ✅
│       └── ...
├── next.config.ts               ✅
├── package.json                 ✅
└── vercel.json                  ✅
```

### Проверка 2: Build Command

1. Settings → General → **"Build & Development Settings"**
2. **Build Command:** должно быть `npm run build` или **пусто**
3. Если указано что-то другое - исправьте

### Проверка 3: Output Directory

1. Settings → General → **"Build & Development Settings"**
2. **Output Directory:** должно быть `.next` или **пусто**
3. Если указано что-то другое - исправьте

### Проверка 4: Переменные окружения

1. Settings → **Environment Variables**
2. Убедитесь, что добавлены:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CRON_SECRET`
3. Все переменные должны быть для **Production, Preview, Development**

---

## 📋 Чеклист

- [ ] Root Directory: **пусто** (не `src`, не `app`)
- [ ] Framework Preset: **Next.js**
- [ ] Build Command: `npm run build` или пусто
- [ ] Output Directory: `.next` или пусто
- [ ] Кеш очищен
- [ ] Новый деплой сделан
- [ ] В логах видны все роуты в "Route (app)"

---

## 🎯 Самый частый случай (90%)

**Проблема:** Root Directory указан как `src`

**Решение:**
1. Settings → General → Root Directory
2. **Удалите значение** (оставьте пустым)
3. Сохраните
4. Clear Build Cache
5. Redeploy

**Это решает проблему в 90% случаев!**

---

## 📞 Если проблема сохраняется

Отправьте мне:
1. **Скриншот Settings → General** (Root Directory, Framework Preset)
2. **Полные логи сборки** (после "npm install" до конца)
3. **Скриншот ошибки** (если есть)

С этой информацией я смогу точно определить проблему!


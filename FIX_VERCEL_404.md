# Пошаговая инструкция: Исправление 404 на Vercel

## ✅ Шаг 1: Проверка локальной сборки (ВЫПОЛНЕНО)

✅ Локальная сборка проходит успешно
✅ Все роуты видны в выводе:
- `/` → редирект на `/collection`
- `/login`
- `/collection`
- `/graph`
- `/settings`
- `/trainer/*`
- `/api/*`

---

## ✅ Шаг 2: Исправление конфигурации (ВЫПОЛНЕНО)

✅ `next.config.ts` - очищен от лишних комментариев
✅ `vercel.json` - правильная конфигурация
✅ Все страницы используют серверные редиректы

---

## 🔧 Шаг 3: Настройки Vercel (ВЫПОЛНИТЕ ЭТИ ШАГИ)

### 3.1. Откройте настройки проекта

1. Перейдите на [Vercel Dashboard](https://vercel.com/dashboard)
2. Найдите проект `knowflow`
3. Нажмите на название проекта
4. Перейдите в **Settings** → **General**

### 3.2. Проверьте Root Directory

**Критически важно!**

1. Найдите секцию **"Root Directory"**
2. **Должно быть:** пусто (`.`) или не указано
3. **НЕ должно быть:** `src`, `app` или любая другая подпапка
4. Если указана подпапка - **удалите значение** (оставьте пустым)
5. Нажмите **"Save"**

### 3.3. Проверьте Framework Preset

1. Найдите секцию **"Framework Preset"**
2. **Должно быть:** `Next.js`
3. Если не `Next.js`:
   - Выберите **"Other"** → **"Next.js"**
   - Или удалите проект и создайте заново с правильным пресетом
4. Нажмите **"Save"**

### 3.4. Проверьте Build Settings

1. Прокрутите вниз до **"Build & Development Settings"**
2. Проверьте:
   - **Build Command:** `npm run build` или пусто
   - **Output Directory:** `.next` или пусто
   - **Install Command:** `npm install` или пусто
3. Если что-то указано неправильно - исправьте
4. Нажмите **"Save"**

### 3.5. Очистите кеш

1. В той же секции **"Build & Development Settings"**
2. Найдите кнопку **"Clear Build Cache"**
3. Нажмите на неё
4. Подтвердите очистку

---

## 🔧 Шаг 4: Переменные окружения

1. Перейдите в **Settings** → **Environment Variables**
2. Убедитесь, что добавлены все 4 переменные:

   - `NEXT_PUBLIC_SUPABASE_URL`
     - Environment: Production, Preview, Development (все три)
   
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
     - Environment: Production, Preview, Development (все три)
   
   - `TELEGRAM_BOT_TOKEN`
     - Environment: Production, Preview, Development (все три)
   
   - `TELEGRAM_CRON_SECRET`
     - Environment: Production, Preview, Development (все три)

3. Если какой-то переменной нет - добавьте её

---

## 🔧 Шаг 5: Новый деплой

### Вариант A: Через Vercel Dashboard

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) справа
4. Выберите **"Redeploy"**
5. Подтвердите редеплой

### Вариант B: Через Git (рекомендуется)

Выполните в терминале:

```bash
cd /Users/nikolajgalickij/knowflow
git add .
git commit -m "Fix Vercel 404 - update configs"
git push origin main
```

Vercel автоматически начнет новый деплой.

---

## 🔍 Шаг 6: Проверка после деплоя

### 6.1. Проверьте логи деплоя

1. Откройте **Deployments** → последний деплой
2. Нажмите **"View Build Logs"**
3. Прокрутите вниз до секции **"Route (app)"**
4. **Должны быть видны все роуты:**
   ```
   Route (app)
   ┌ ○ /
   ├ ○ /_not-found
   ├ ƒ /api/generate-flashcards
   ├ ƒ /api/telegram/generate-link
   ├ ƒ /api/telegram/send-cards
   ├ ƒ /api/telegram/webhook
   ├ ○ /collection
   ├ ○ /graph
   ├ ○ /login
   ├ ○ /settings
   ├ ○ /trainer
   ├ ○ /trainer/cards
   ├ ○ /trainer/challenges
   ├ ○ /trainer/graph
   └ ○ /trainer/study
   ```

### 6.2. Проверьте URL

Откройте в браузере:
- `https://your-app.vercel.app/login` - должна открыться страница логина
- `https://your-app.vercel.app/collection` - должна открыться страница коллекции
- `https://your-app.vercel.app/` - должен быть редирект на `/collection`

### 6.3. Проверьте консоль браузера

1. Откройте страницу (F12)
2. Перейдите в **Console**
3. Не должно быть ошибок 404

---

## 🚨 Если проблема сохраняется

### Проверка 1: Сравните настройки

Убедитесь, что в Vercel:
- ✅ Root Directory: **пусто**
- ✅ Framework Preset: **Next.js**
- ✅ Build Command: `npm run build` или **пусто**
- ✅ Output Directory: `.next` или **пусто**

### Проверка 2: Проверьте структуру проекта

Убедитесь, что структура правильная:
```
knowflow/
├── src/
│   └── app/
│       ├── layout.tsx          ✅
│       ├── page.tsx             ✅
│       ├── login/
│       │   └── page.tsx         ✅
│       └── ...
├── next.config.ts               ✅
├── package.json                 ✅
└── vercel.json                  ✅
```

### Проверка 3: Пересоздайте проект

Если ничего не помогает:

1. **Экспортируйте переменные окружения:**
   - Settings → Environment Variables
   - Запишите все значения

2. **Удалите проект:**
   - Settings → General
   - Прокрутите вниз
   - Нажмите "Delete Project"

3. **Создайте новый проект:**
   - "Add New" → "Project"
   - Импортируйте репозиторий
   - **Важно:** При создании выберите **"Next.js"** как Framework
   - Root Directory: **оставьте пустым**

4. **Добавьте переменные окружения:**
   - Settings → Environment Variables
   - Добавьте все 4 переменные

5. **Задеплойте:**
   - Vercel автоматически начнет деплой

---

## ✅ Чеклист перед деплоем

- [ ] Root Directory в Vercel: **пусто**
- [ ] Framework Preset: **Next.js**
- [ ] Build Command: `npm run build` или **пусто**
- [ ] Output Directory: `.next` или **пусто**
- [ ] Все 4 переменные окружения добавлены
- [ ] Кеш очищен
- [ ] Локальная сборка проходит успешно (`npm run build`)
- [ ] Все роуты видны в локальной сборке

---

## 📞 Если нужна помощь

Если проблема сохраняется после всех шагов:

1. **Скопируйте логи деплоя:**
   - Deployments → последний деплой → View Build Logs
   - Скопируйте весь вывод

2. **Проверьте настройки:**
   - Сделайте скриншот Settings → General

3. **Сравните с локальной сборкой:**
   - Запустите `npm run build` локально
   - Убедитесь, что вывод совпадает

---

## 🎯 Самый частый случай (90% проблем)

**Проблема:** Root Directory указан как `src` или другая подпапка

**Решение:**
1. Settings → General → Root Directory
2. **Оставить пустым** (удалить значение)
3. Сохранить
4. Clear Build Cache
5. Redeploy

**Это решает 90% проблем с 404 на Vercel!**

